{
  "name": "edverse",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "prepare": "echo 'Build preparation complete'",
    "lint": "npm run lint --workspaces --if-present"
  }
}
